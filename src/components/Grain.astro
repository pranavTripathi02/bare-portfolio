---
import grainSvg from "../assets/grain.svg?url";

type Props = {
    opacity: number;
};

const { opacity } = Astro.props;
---

<div
    style={{ opacity, backgroundImage: `url(${grainSvg})`,}}
    class="pointer-events-none fixed inset-0 select-none mix-blend-difference"
>
</div>
