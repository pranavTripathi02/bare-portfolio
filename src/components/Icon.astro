---
interface Props {
    icon: string;
}
// console.log("in icon")

const {icon} = Astro.props as Props;
const {default:innerHtml} = await import(`/src/assets/${icon}.svg?raw`)
---

<Fragment set:html={innerHtml} />
